# 구독한잔해 구현 과정

## 2026-09-18

### 1. 구현 시작
- 소프트웨어 설계서를 기준으로 구현을 시작함.
- Python 3.13.7 기반 CLI 프로그램으로 구현함.
- 기능별로 코드를 작성하고 테스트하면서 진행할 예정임.

### 2. JSON 데이터 구조 작성
- 구독 정보를 저장하기 위한 subscriptions.json 파일 작성.
- 서비스명을 Key로 사용함.
- 저장 항목:
  - cost
  - cycle
  - next_payment
  - category
  - last_used
  - trial_end
  - shared_count

### 3. 결제 주기 정규화 기능 구현

- payment_normalizer.py 파일 작성.
- to_monthly(cost, cycle) 함수 구현.
- 월간: cost 그대로 사용
- 연간: cost / 12
- 4주: cost × 13 / 12

#### 막힌 부분
- Python에서 함수를 정의하는 문법이 익숙하지 않아 함수 작성 방식에서 막힘.
- payment_normalizer.py에 작성한 함수를 main.py에서 사용하는 import 방법을 몰라 막힘.

### 4. 구독 등록 기능 구현

- subscriptions.py 파일 작성.
- 구독 정보를 저장하기 위해 subscriptions = {} 딕셔너리 생성.
- register_subscription() 함수 작성.
- 콘솔에서 다음 정보를 순서대로 입력받도록 구현.
  - 서비스명
  - 요금
  - 결제 주기
  - 다음 결제 예정일
  - 카테고리
  - 마지막 이용일
  - 무료체험 종료일
  - 공유 인원수
- 입력받은 서비스명을 Key로 사용하고 나머지 정보를 Value로 하여 subscriptions 딕셔너리에 저장하도록 구현.
- 현재는 정상 입력 기준으로 기본 등록 기능을 먼저 구현하고, 입력값 검증 및 예외 처리는 추후 추가할 예정.

#### 막힌 부분
- 콘솔에서 입력받은 값을 기존 `subscriptions` 딕셔너리에 저장하는 방법에서 막힘.
- 구독 입력 함수를 `main.py`와 `subscriptions.py` 중 어느 파일에 둘지 고민함.

#### 해결
- `subscriptions[service_name] = {...}` 형식으로 입력받은 값을 딕셔너리에 추가하는 방법을 확인함.
- 구독 등록 기능은 subscriptions.py에 두고, main.py에서는 해당 함수를 호출하는 구조로 구성하기로 함.
#### 해결
- 함수는 `def 함수명(매개변수):` 형식으로 정의하는 것을 확인함.
- 다른 파일의 함수는 from payment_normalizer import to_monthly 방식으로 불러올 수 있음을 확인함.

### 5. 구독 조회 기능 구현

- subscriptions.py 파일 작성.
- view_subscriptions 함수 구현.
- subscriptions.item에서 key와 vaule으로 가져와서 서비스명, 요금, 결제주기, 다음 결제 , 카테고리를 출력하는 것을 반복하도록 구현
- 구독 감지 함수 구현


#### 막힌 부분
- 이중 딕셔너리 구조인데 어떻게 꺼내서 출력하는지 막힘.




#### 해결
- 임시 딕셔너리인 value와 임시 변수인 key를 for문으로 출력함.


### 6. 분담 비용 계산기 구현
 - shared_cost_calculator.py 파일 작성.
 - caculator() 함수 구현.
 - subscription에서 공유 인원수, 비용, 서비스명을 shared_cost_calculator.py 파일에 가져옴.
 - to_monthly 함수에서 공유 인원수와 비용으로 월 환산 비용 만듬.
 - 월 환산 비용/서비스명으로 1인당 월 실 부담액을 만듬.
 - 1인당 실 부담액, 공유 인원수, 월 환산 비용, 서비스 명을 출력.

  #### 막힌 부분
  - 딕셔너리의 키 값을 어떻게 다른 파일로 가져오는 지 고민함.
  - 문자열로 저장된 값들을 어떻게 계산하는 지 고민함.

   #### 해결 
   - subscriptions[service_name]["shared_count"] 방식으로 딕셔너리명, 키, 값 형태로 가져오는 것으로 해결.
   - 파이썬에는 자료형이 없어도 되는 줄 알았는데 앞에 float() 처럼 함수를 사용하는 것으로 해결. 
 
### 7. 메인 화면 구현

- main.py 파일에 프로그램의 메인 메뉴 화면 작성.
- 사용자가 실행할 기능을 선택할 수 있도록 다음 메뉴를 출력하도록 구현.
    구독 관리
    결제 주기 정규화
    체감 지출 리포트
    분담비용 계산기
    저장 후 종료
- 사용자가 입력한 메뉴 번호에 따라 각 기능의 함수를 호출하도록 구성.
- 구독 관리 기능은 subscription_management.py의 subscription_management() 함수를 호출하도록 연결.
- 분담비용 계산기는 shared_cost_calculator.py의 `calculator()` 함수를 호출하도록 연결.
- 아직 구현하지 않은 기능은 추후 각각의 함수와 연결할 예정임.

#### 막힌 부분
- 없음



#### 해결
- 없음





### 8. 구독 관리 화면 구현

- subscription_management.py 파일 작성.
- 구독 등록, 조회, 삭제 기능을 선택할 수 있는 구독 관리 메뉴 화면 구현.
- 다음 메뉴를 출력하도록 구성.
     구독 등록
     구독 조회
     구독 삭제
     뒤로가기
- 사용자가 입력한 메뉴 번호에 따라 해당 함수를 호출하도록 구현.
- 구독 등록 선택 시 register_subscription() 함수 호출.
- 구독 조회 선택 시 view_subscriptions() 함수 호출.
- 구독 삭제 기능은 추후 구현 후 연결할 예정.
- 뒤로 가기 선택 시 함수 종료.


#### 막힌 부분
- 없음

#### 해결
- 없음



### 9. 중간 점검
- 설계 할 때 어떻게 만들 지는 만들었는데 구현 순서가 만들 지 않았다. 지금은 하고 싶은 데로 만들고 있는데 다음에는 구현 순서도 설계서에 고려해봐야겠다.
- 주석 열심히 달아야겠다.


### 10. 구독 삭제
- subscriptions.py에 delete_subscription() 함수 구현.
- del 예약어를 이용해서 키와 값을 삭제.

#### 막힌 부분
- 어떻게 키와 값을 삭제하는 것에서 막힘.

#### 해결
- del 딕셔너리[키] 방식으로 삭제 할 수 있다는 것을 알게 됨.



## 2026년 9월 19일


### 11. JSON 저장 기능 구현
- subscriptions.py에 save_subscriptions() 함수 구현.
- json 모듈을 이용해서 subscriptions 딕셔너리를 subscriptions.json 파일에 저장하도록 구현.
- 메인 메뉴에서 0번 저장 후 종료를 선택하면 저장 함수가 실행되도록 연결.

#### 막힌 부분
- 딕셔너리의 내용을 JSON 파일에 저장하는 코드를 어떻게 작성하는지 몰라서 막힘.

#### 해결
- AI에게 JSON 저장 방법을 질문하여 `json.dump()`를 이용하는 방법을 확인함.
- `with open()`으로 JSON 파일을 열고 `json.dump()`를 이용하여 subscriptions 딕셔너리를 저장하도록 구현함.



### 12. 해지 후보 탐지

- subscriptions.py에 detect_cancellation_candidates() 함수 구현.
- datetime을 이용해서 현재 날짜와 마지막 이용일의 차이를 계산.
- 마지막 이용 후 30일을 초과한 구독을 해지 후보로 출력하도록 구현.
- 구독 조회가 끝난 뒤 해지 후보 탐지 함수가 자동으로 실행되도록 연결.

#### 막힌 부분
- 문자열로 저장된 날짜를 계산하는 방법을 몰라서 막힘.
- 현재 날짜와 마지막 이용일의 차이를 구하는 방법을 몰랐음.
- 해지 후보가 하나도 없는 경우를 어떻게 판단할지 몰랐음.

#### 해결
- AI에게 날짜 계산 방법을 질문하여 `datetime.strptime()`으로 문자열을 날짜 형태로 변환하는 방법을 확인함.
- `(current_date - last_used).days` 방식으로 경과 일수를 계산함.
- `candidate_found` 변수를 이용하여 해지 후보가 존재하는지 확인하도록 구현함.

### 13. 체감 지출 리포트

- spending_report.py 파일 작성
- spending_report 함수 구현
- 예상 월간 예상 지출액을 to_monthly에서 딕셔너리의 cost와 cycle을 이용해서 구함
- 월간 지출액을 12배해서 연간 지출액을 만들고 이를 출력하고 5000으로 나눠서 체감 지출을 만듬.

#### 막힌 부분
- 없음

#### 해결
- 없음

### 14. 구독 불러오기 
- load_subscriptions 함수 구현함.

#### 막힌 부분
- 딕셔너리의 내용을 JSON 파일에 불러오기 하는 코드를 어떻게 작성하는지 몰라서 막힘.


#### 해결
- AI에게 JSON 불러오기 방법을 질문하여 `json.load()`를 이용하는 방법을 확인함.
- `with open()`으로 JSON 파일을 읽기 모드로 열고 `json.load()`를 이용하여 저장된 데이터를 불러오도록 구현함.
- 불러온 데이터를 `subscriptions.update()`를 이용하여 기존 subscriptions 딕셔너리에 추가하도록 구현함.

### 15. 결제 주기 정규화
- normalize_subscriptions 구현
- 서비스별 비용이랑 카테고리 별 월 지출을 출력 할 수 있도록 구현함.

#### 막힌 부분
- 카테고리 별 월 지출 구할 때 서로 다른 카테고리를 한 번에 월 환산 계산 하는 것이 어려움.

#### 해결
- 처음에는 switch문을 사용하려고 했지만 Python 실행 버전이 불확실하여 if-elif문을 사용함.
- 각 구독의 category 값을 확인한 뒤, 해당 카테고리에 맞는 변수에 월 환산 비용을 누적하는 방식으로 구현함.
- 
## 2026년 9월 20일

### 16. 포기 하지 않음

#### 막힌 부분
- 기존 폴더를 덮어쓰기 해서 코드 사라지고 업데이트 안된 부분 발생.

#### 해결
- git 구현과정이랑 AI한테 질문하고 받은 걸 바탕으로 재현 요청.
- 코드틀린 부분이나 충돌 발생 한 부분 있는지 검증.



### 17. 중간 점검2

코드 복구 후 각 파일의 기능 연결, 출력 문구, 계산식 등을 다시 점검함.

#### 1) 체감 지출 리포트 나눗셈 수정
- 커피 환산 잔 수를 계산할 때 `//`를 사용하여 소수점 이하가 버려지는 문제를 발견함.
- 설계서에서는 커피 환산 결과가 162.3잔과 같이 소수점으로 출력되어야 하므로 `/` 연산자로 수정함.

#### 2) payment_normalizer.py import 누락 수정
- `normalize_subscriptions()`에서 subscriptions 딕셔너리를 사용하지만 import가 빠져 있는 것을 발견함.
- `from subscriptions import subscriptions`를 추가함.

#### 3) 메인 기능 연결 점검
- main.py에서 구독 관리, 결제 주기 정규화, 체감 지출 리포트, 분담비용 계산기가 각각 올바른 함수와 연결되어 있는지 다시 확인함.
- 프로그램 시작 시 `load_subscriptions()`가 실행되고, 종료 시 `save_subscriptions()`가 실행되는 것도 확인함.

#### 4) 결제 주기 정규화 출력 수정
- 서비스별 월 환산 비용 출력 형식을 설계서에 맞게 수정함.
- `전체 환산 비용`을 `전체 월 지출`로 수정함.
- 카테고리별 출력 제목을 `[카테고리별 월 지출]`로 수정함.
- 전체 월 지출 출력문이 반복문 안에 들어가 있던 들여쓰기 오류를 발견하여 반복문 밖으로 수정함.

#### 5) 구독 등록 및 삭제 화면 수정
- 구독 등록 화면에 `===== 구독 등록 =====` 제목을 추가함.
- 요금 입력 문구가 잘못 변경된 부분을 다시 `요금을 입력해주세요:`로 수정함.
- 다음 결제 예정일과 마지막 이용일 입력 시 날짜 형식 `(YYYY-MM-DD)`을 표시하도록 수정함.
- 구독 삭제 화면에 `===== 구독 삭제 =====` 제목을 추가함.

#### 6) 해지 후보 및 무료체험 출력 문구 수정
- 해지 후보가 존재할 경우 `===== 해지 후보 =====` 제목이 출력되도록 수정함.
- 해지 후보와 무료체험 종료 정보를 설계서의 출력 형태에 맞게 수정함.
- 해지 후보가 하나도 없는 경우에는 `해지 후보가 없습니다.`가 출력되도록 추가함.

#### 7) 분담비용 계산기 화면 수정
- 계산할 서비스를 입력하기 전에 현재 등록된 구독 서비스 목록을 출력하도록 추가함.
- 계산 결과를 출력하기 전에 `===== 분담비용 계산 결과 ====` 제목이 나오도록 수정함.

#### 8) 프로그램 출력 문구 점검
- 메인 메뉴의 `선택:`이라는 문구가 다른 입력 문구와 어울리지 않아 `메뉴를 선택해주세요:`로 수정함.
- 구독 관리 메뉴도 같은 문구를 사용하도록 통일함.
- 체감 지출 리포트의 `커피 5000원 기준`을 `커피 5,000원 기준`으로 수정함.

#### 점검 결과
- 구독 등록, 조회, 삭제 기능 연결 확인.
- 결제 주기 정규화 기능 연결 확인.
- 체감 지출 리포트 기능 연결 확인.
- 분담비용 계산기 기능 연결 확인.
- JSON 저장 및 불러오기 기능 연결 확인.
- 기본 기능과 출력 문구 점검을 완료함.
- 입력값 검증 및 예외 처리는 아직 구현하지 않았으며 다음 단계에서 진행할 예정.

### 18. 정상 테스트

 #### 1. 개발자가 테스트 기준일을 2026-09-09로 정하고, 구독이 없는 상태에서 프로그램을 실행한다.
 #### 2. 구독 등록 기능으로 표의 6개 구독에 대한 8개 항목을 입력한다. 무료체험 종료일이 없는 경우 입력을 생략하고, 등록 완료 메시지를 확인한다.
 #### 3. 구독 조회를 선택하여 6개 구독이 출력되고, 해지 후보 탐지가 자동으로 실행되는지 확인한다.   -> 통과
 
  <img width="751" height="1402" alt="image" src="https://github.com/user-attachments/assets/0aeed3c5-3a35-41ae-8d65-687b3c596187" />
테스트3 성공)


 #### 4. 멜론(46일)과 테스트 OTT 구독(39일)만 해지 후보로 표시되고, 정확히 30일이 지난 Microsoft 365는 제외되는지 확인한다. 테스트 OTT 구독의 무료체험 알림은 D-11인지 확인한다.-> 실패 

#### 실패한 이유

- 1) 기획서 4번은 기준일을 2026-09-09로 잡았는데 내 코드에선 현재 날짜 기준이었다.

- 2) 후보 출력문이 if not candidate_found: 안쪽에 들어가 있어서 첫 번째 후보만 출력되고 그 뒤 후보는 안 나오는 상태(들여 쓰기 실수)



#### 해결

- 1) 기획서 기준일을 2026-09-09로 변경

- 2) 들여쓰기 수정


<img width="884" height="352" alt="image" src="https://github.com/user-attachments/assets/5b99c2ff-2353-4697-88e1-35dc5fe6701f" />

테스트4 수정전)


<img width="740" height="342" alt="image" src="https://github.com/user-attachments/assets/569ef6fc-a709-4a4d-bd44-5376fcde2537" />
테스트4 수정후)
 
#### 5. 결제 주기 정규화를 선택하여 전체 월 지출이 약 75,716.67원이고, 같은 OTT 카테고리의 비용이 27,000원으로 합산되는지 확인한다. -> 실패

#### 실패한 이유: 
- 돈 입력할 때 ,을 넣음

#### 해결
- 돈 입력할 때 숫자만 넣음


<img width="982" height="387" alt="image" src="https://github.com/user-attachments/assets/cdc452d0-099c-4246-bab6-f810cbd324fa" />
테스트5 수정전)


<img width="685" height="363" alt="image" src="https://github.com/user-attachments/assets/96779707-c9ff-4c0a-9ee9-98cb8a2983a5" />
테스트5 수정후)


#### 6. 체감 지출 리포트를 선택하여 연간 예상 지출액 908,600원과 커피 환산 결과 181.72잔이 출력되는지 확인한다.->  성공


#### 7. 분담비용 계산에서 넷플릭스를 선택하여 저장된 공유 인원수 2명으로 계산한 월 실부담액 8,500원이 출력되는지 확인한다.-> 성공


#### 8. Microsoft 365를 삭제하고 다시 조회하여 나머지 5개 구독만 남아 있는지 확인한다. -> 성공


### 19. 메인 메뉴 입력 예외처리
- main.py의 메인 메뉴 입력 예외 처리 구현.
- 아무것도 입력하지 않고 Enter를 누른 경우 빈 문자열인지 확인하도록 구현.
- 빈 값을 입력하면 메뉴 번호를 입력해주세요. 0~4 중 하나의 번호를 입력해주세요.를 출력하고 다시 입력받도록 구현.
- 0, 1, 2, 3, 4 이외의 값이 입력되면 잘못된 메뉴 입력입니다. 0~4 중 하나의 번호만 입력해주세요.를 출력하고 다시 입력받도록 구현.
- 문자 , 특수문자, 소수, 범위를 벗어난 숫자 등의 입력이 메뉴 기능으로 넘어가지 않도록 처리 되도록 구현.

#### 막힌 부분
- 잘못된 값을 입력했을 때 프로그램을 종료하지 않고 다시 메뉴 입력으로 돌아가게 하는 방법을 고민함.
- 아무것도 입력하지 않고 Enter만 누른 경우를 어떻게 구분하는지 몰랐음.
- 연속 입력이나 공백을 입력 버퍼에 안 쌓이고 제거 하는 방법.

#### 해결 방법
- AI한테 물어 봐서 continue로 돌아가는 방법, 공백제거 하는 방법 알아 냄.


### 20. 메뉴 입력 예외처리
- subscription_management.py의 구독 관리 메뉴 입력 예외 처리 구현.
- 아무것도 입력하지 않고 Enter를 누른 경우 빈 문자열인지 확인하도록 구현.
-  빈 값을 입력하면 메뉴 번호를 입력해주세요. 0~3 중 하나의 번호를 입력해주세요.를 출력하고 다시 입력 받도록 구현.
- 0, 1, 2, 3 이외의 값이 입력되면 잘못된 메뉴 입력입니다. 0~3 중 하나의 번호를 입력해주세요.를 출력하고 다시 입력받도록 구현.
- 문자, 특수문자, 소수, 범위를 벗어난 숫자 등의 입력이 메뉴 기능으로 넘어가지 않도록 처리되도록 구현.

#### 막힌 부분
- 12, 1 2처럼 여러 값을 한 번에 입력은 잘못된 메뉴 입력입니다. `0~3` 중 하나의 번호만 입력해주세요. 라고 되어 있고
  나머지는 잘못된 메뉴 입력입니다. `0~3` 중 하나의 번호`를` 입력해주세요. 되어 있어서 구현이 어려움.

#### 해결 방법
- 구문을 통일함.

  

